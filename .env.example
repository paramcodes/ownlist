# ── Backend ─────────────────────────────────────────────────────
APP_SECRET=secret               # Application secret (used for JWT signing)
OMDB_API_KEY=5622a1e6             # OMDb API key (https://www.omdbapi.com/)

# ── Database ───────────────────────────────────────────────────
DATABASE_URL=mongodb+srv://course:xkLYUkN1UnykBk8z@cluster0.eyxcg4p.mongodb.net/?appName=Cluster0             # MySQL connection string (mysql://user:pass@host:port/db)

# ── Admin Role ──────────────────────────────────────────────────
OWNER_UNION_ID=param           # Union ID of the app creator; this user gets role "admin" on first login
